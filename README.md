# LLM-Classification-Finetuning
Predict which LLM response users will prefer in head-to-head chatbot battles from the Chatbot Arena. Given a user prompt and two model responses (A and B), output the probability of each outcome: Model A wins, Model B wins, or Tie. This task mirrors the concept of reward models / preference models used in RLHF.
